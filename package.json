{
  "name": "webcli",
  "version": "1.2.0",
  "description": "A CLI-like navigation tool for websites that listens to key events and executes commands.",
  "repository": {
    "type": "git",
    "url": "https://github.com/patrik-manyone/web-cli.git"
  },
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && npm run copy-static",
    "copy-static": "copyfiles -u 1 \"examples/**/*.html\" public/examples",
    "start": "python3 -m http.server 81 --directory public"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^4.9.5"
  }
}
